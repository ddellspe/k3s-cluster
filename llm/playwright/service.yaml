apiVersion: v1
kind: Service
metadata:
  name: playwright-service
  namespace: llm
  labels:
    app: playwright
spec:
  type: ClusterIP
  selector:
    app: playwright
  ports:
  - name: ws
    port: 3000
    targetPort: 3000
    protocol: TCP
