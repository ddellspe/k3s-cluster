apiVersion: v1
kind: Service
metadata:
  name: llm-nemotron-service
  namespace: llm
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  selector:
    app: llm-nemotron
