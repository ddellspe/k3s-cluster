apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - rbac.yaml
  - secret.yaml
  - configmaps.yaml
  - services.yaml
  - daemonset.yaml
  - statefulset-vm.yaml
  - deployment-grafana.yaml
