apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev-infra

resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: zot-config
    files:
      - config.json
    options:
      disableNameSuffixHash: true
